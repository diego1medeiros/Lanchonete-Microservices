server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/diego1medeiros/microservices-configserver
          search-paths: fornecedor,cliente,funcionario,produto,venda
 
          
           
    